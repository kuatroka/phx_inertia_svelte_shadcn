<svg width="160" height="32" xmlns="http://www.w3.org/2000/svg">
  <!-- Empty block -->
  <rect x="0" y="0" width="16" height="16" fill="#9bbc0f"/>
  
  <!-- I piece (cyan) -->
  <rect x="16" y="0" width="16" height="16" fill="#306230"/>
  
  <!-- O piece (yellow) -->
  <rect x="32" y="0" width="16" height="16" fill="#8bac0f"/>
  
  <!-- T piece (purple) -->
  <rect x="48" y="0" width="16" height="16" fill="#0f380f"/>
  
  <!-- S piece (green) -->
  <rect x="64" y="0" width="16" height="16" fill="#306230"/>
  
  <!-- Z piece (red) -->
  <rect x="80" y="0" width="16" height="16" fill="#0f380f"/>
  
  <!-- J piece (blue) -->
  <rect x="96" y="0" width="16" height="16" fill="#306230"/>
  
  <!-- L piece (orange) -->
  <rect x="112" y="0" width="16" height="16" fill="#8bac0f"/>
  
  <!-- Ghost piece -->
  <rect x="128" y="0" width="16" height="16" fill="none" stroke="#306230" stroke-width="2"/>
  
  <!-- Wall/border -->
  <rect x="144" y="0" width="16" height="16" fill="#0f380f"/>
  
  <!-- Second row for variations -->
  <!-- Filled block -->
  <rect x="0" y="16" width="16" height="16" fill="#0f380f"/>
  
  <!-- Grid lines -->
  <rect x="16" y="16" width="16" height="16" fill="#9bbc0f" stroke="#8bac0f" stroke-width="1"/>
</svg>
